/**
 * CookiePolicy.tsx
 * מדיניות קוקיז — ReviewHub
 * עומד ב: חוק התקשורת (בזק ושידורים) התשמ"ב-1982 (סעיף 30א),
 *          GDPR ePrivacy Directive, הנחיות רשות הגנת הפרטיות הישראלית
 * נתיב: /cookie-policy
 */
import { useState } from "react";
import { motion, AnimatePresence } from "framer-motion";
import { Cookie, ChevronDown, Mail, ExternalLink } from "lucide-react";
import Navbar from "@/components/Navbar";
import Footer from "@/components/Footer";

const META = { updated: "10 במרץ 2026", email: "privacy@reviewhub.co.il" };

const COOKIE_CATEGORIES = [
  {
    name: "חיוניות (Essential)",
    badge: "חובה — לא ניתן לכיבוי",
    badgeColor: "bg-emerald-500/10 text-emerald-400 border-emerald-500/20",
    desc: "הכרחיות לתפקוד האתר. ללא קוקיז אלה, האתר לא יפעל כראוי.",
    cookies: [
      { name: "sb-pujsopidbejeuqteormi-auth-token", purpose: "ניהול הפעלת משתמש מחובר (JWT)", duration: "1 שנה", provider: "Supabase" },
      { name: "sb-pujsopidbejeuqteormi-auth-token-code-verifier", purpose: "PKCE OAuth flow", duration: "הפעלה", provider: "Supabase" },
      { name: "rh-csrf", purpose: "הגנה מפני CSRF attacks", duration: "הפעלה", provider: "ReviewHub" },
    ],
  },
  {
    name: "פונקציונליות (Functional)",
    badge: "אופציונלי",
    badgeColor: "bg-blue-500/10 text-blue-400 border-blue-500/20",
    desc: "משפרות את חוויית השימוש. ניתן לסירוב ללא פגיעה בפונקציות הליבה.",
    cookies: [
      { name: "rh-cookie-consent-v1", purpose: "שמירת העדפות הסכמה לקוקיז", duration: "1 שנה", provider: "ReviewHub" },
      { name: "rh-theme", purpose: "שמירת העדפת מצב כהה/בהיר", duration: "1 שנה", provider: "ReviewHub" },
    ],
  },
  {
    name: "ניתוח (Analytics)",
    badge: "אופציונלי",
    badgeColor: "bg-yellow-500/10 text-yellow-400 border-yellow-500/20",
    desc: "עוזרות לנו להבין כיצד המשתמשים מנהלים את עצמם באתר, לצורך שיפור.",
    cookies: [
      { name: "_ga", purpose: "Google Analytics — מזהה משתמש ייחודי לניתוח תנועה", duration: "2 שנים", provider: "Google" },
      { name: "_gid", purpose: "Google Analytics — הבחנה בין משתמשים", duration: "24 שעות", provider: "Google" },
      { name: "_gat", purpose: "Google Analytics — הגבלת קצב בקשות", duration: "1 דקה", provider: "Google" },
    ],
  },
  {
    name: "שיווקיות (Marketing)",
    badge: "לא בשימוש",
    badgeColor: "bg-white/5 text-white/30 border-white/10",
    desc: "אין שימוש בקוקיז שיווקיות, פרסום ממוקד או pixel tracking כלשהו באתר.",
    cookies: [],
  },
];

const SECTIONS = [
  {
    title: "מהם קובצי עוגיות?",
    body: [
      { type: "p", text: 'קובצי עוגיות (cookies) הם קבצי טקסט קטנים שמוצבים על מכשירך בעת ביקור באתר. הם מאפשרים לאתר לזכור אותך, לשמור הגדרות, ולשפר את חוויתך.' },
      { type: "p", text: 'בנוסף לקוקיז, אנחנו משתמשים ב-localStorage לאחסון מקומי של הגדרות UI בלבד (ערכת צבעים, פילטרים). מידע זה לעולם אינו נשלח לשרת שלנו.' },
    ],
  },
  {
    title: "Google Analytics — פרטיות",
    body: [
      { type: "p", text: "אנו משתמשים ב-Google Analytics עם ההגדרות הבאות לשמירה על פרטיותך:" },
      { type: "list", items: [
        "IP Anonymization מופעל — ה-IP שלך לעולם אינו מועבר ל-Google במלואו",
        "Data Retention: 14 חודשים בלבד",
        "אין שיתוף נתונים למטרות פרסום",
        "אין Remarketing",
      ]},
      { type: "p", text: "לביטול מעקב Google Analytics לגמרי: התקן את ה-Opt-out Add-on של Google." },
    ],
  },
  {
    title: "כיצד לנהל ולמחוק קוקיז",
    body: [
      { type: "subtitle", text: "דרך האתר:" },
      { type: "p", text: 'לחץ על "ניהול קוקיז" בפוטר של כל עמוד לעדכון הסכמותיך בכל עת.' },
      { type: "subtitle", text: "דרך הדפדפן:" },
      { type: "list", items: [
        "Chrome: הגדרות → פרטיות ואבטחה → קובצי Cookie ונתוני אתרים",
        "Firefox: הגדרות → פרטיות ואבטחה → עוגיות ונתוני אתרים",
        "Safari: העדפות → פרטיות → נהל נתוני אתר",
        "Edge: הגדרות → קובצי Cookie והרשאות לאתר",
      ]},
      { type: "p", text: "שים לב: מחיקת קוקיז חיוניות תנתק אותך מהחשבון." },
    ],
  },
  {
    title: "שינויים במדיניות",
    body: [
      { type: "p", text: `שינויים מהותיים יפורסמו דרך באנר הסכמה חדש. לשאלות: ${META.email}` },
    ],
  },
];

const AccordionItem = ({ section }: { section: typeof SECTIONS[0] }) => {
  const [open, setOpen] = useState(false);
  return (
    <div className="border border-white/10 rounded-2xl overflow-hidden bg-white/[0.03]">
      <button onClick={() => setOpen(v => !v)} className="w-full flex items-center justify-between px-6 py-4 text-right hover:bg-white/[0.04] transition-colors" aria-expanded={open}>
        <span className="font-semibold text-white text-sm">{section.title}</span>
        <ChevronDown size={16} className={`text-white/40 transition-transform duration-200 shrink-0 ml-4 ${open ? "rotate-180" : ""}`} />
      </button>
      <AnimatePresence initial={false}>
        {open && (
          <motion.div initial={{ height: 0, opacity: 0 }} animate={{ height: "auto", opacity: 1 }} exit={{ height: 0, opacity: 0 }} transition={{ duration: 0.2 }} className="overflow-hidden">
            <div className="px-6 pb-5 pt-1 border-t border-white/10 space-y-3">
              {section.body.map((block, i) => {
                if (block.type === "p") return <p key={i} className="text-sm text-white/60 leading-relaxed">{block.text}</p>;
                if (block.type === "subtitle") return <p key={i} className="text-sm font-semibold text-white/80 mt-2">{block.text}</p>;
                if (block.type === "list") return (
                  <ul key={i} className="space-y-1.5">
                    {(block.items as string[]).map((item, j) => (
                      <li key={j} className="flex gap-2 text-sm text-white/60"><span className="text-emerald-400 shrink-0 mt-0.5">•</span><span>{item}</span></li>
                    ))}
                  </ul>
                );
                return null;
              })}
            </div>
          </motion.div>
        )}
      </AnimatePresence>
    </div>
  );
};

export default function CookiePolicy() {
  return (
    <div className="min-h-screen bg-background" dir="rtl">
      <Navbar />
      <main className="container max-w-2xl py-16">
        <motion.div initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }} className="text-center mb-12">
          <div className="inline-flex items-center gap-2 bg-emerald-500/10 border border-emerald-500/20 text-emerald-400 px-4 py-1.5 rounded-full text-xs font-semibold mb-5">
            <Cookie size={13} /> שקיפות מלאה
          </div>
          <h1 className="text-3xl font-bold text-white mb-3">מדיניות קובצי עוגיות</h1>
          <p className="text-white/50 text-sm">עודכן: {META.updated}</p>
        </motion.div>

        {/* Cookie categories with table */}
        <div className="space-y-4 mb-8">
          {COOKIE_CATEGORIES.map((cat, i) => (
            <motion.div key={i} initial={{ opacity: 0, y: 10 }} animate={{ opacity: 1, y: 0 }} transition={{ delay: i * 0.06 }}
              className="border border-white/10 rounded-2xl overflow-hidden bg-white/[0.03]">
              <div className="flex items-start gap-3 p-4">
                <span className={`text-[11px] font-semibold px-2.5 py-1 rounded-full border shrink-0 mt-0.5 ${cat.badgeColor}`}>
                  {cat.badge}
                </span>
                <div>
                  <p className="font-semibold text-white text-sm">{cat.name}</p>
                  <p className="text-xs text-white/50 mt-0.5 leading-relaxed">{cat.desc}</p>
                </div>
              </div>
              {cat.cookies.length > 0 && (
                <div className="border-t border-white/10 overflow-x-auto">
                  <table className="w-full text-xs">
                    <thead>
                      <tr className="bg-white/[0.03]">
                        <th className="text-right px-4 py-2.5 text-white/50 font-semibold">שם</th>
                        <th className="text-right px-4 py-2.5 text-white/50 font-semibold">מטרה</th>
                        <th className="text-right px-4 py-2.5 text-white/50 font-semibold">תוקף</th>
                        <th className="text-right px-4 py-2.5 text-white/50 font-semibold">ספק</th>
                      </tr>
                    </thead>
                    <tbody>
                      {cat.cookies.map((c, j) => (
                        <tr key={j} className="border-t border-white/[0.06]">
                          <td className="px-4 py-2.5 font-mono text-emerald-400/80 text-[10px]">{c.name}</td>
                          <td className="px-4 py-2.5 text-white/60">{c.purpose}</td>
                          <td className="px-4 py-2.5 text-white/50">{c.duration}</td>
                          <td className="px-4 py-2.5 text-white/50">{c.provider}</td>
                        </tr>
                      ))}
                    </tbody>
                  </table>
                </div>
              )}
            </motion.div>
          ))}
        </div>

        <div className="space-y-2">
          {SECTIONS.map((s, i) => (
            <motion.div key={i} initial={{ opacity: 0, y: 8 }} animate={{ opacity: 1, y: 0 }} transition={{ delay: 0.3 + i * 0.03 }}>
              <AccordionItem section={s} />
            </motion.div>
          ))}
        </div>

        {/* External links */}
        <motion.div initial={{ opacity: 0 }} animate={{ opacity: 1 }} transition={{ delay: 0.6 }}
          className="mt-6 p-4 bg-white/[0.03] border border-white/10 rounded-2xl">
          <p className="text-xs text-white/40 mb-3 font-semibold">קישורים שימושיים</p>
          <div className="space-y-2">
            {[
              { text: "ביטול מעקב Google Analytics", href: "https://tools.google.com/dlpage/gaoptout" },
              { text: "הרשות להגנת הפרטיות בישראל", href: "https://www.gov.il/he/departments/the_privacy_protection_authority" },
              { text: "מדיניות פרטיות Google", href: "https://policies.google.com/privacy" },
            ].map((l, i) => (
              <a key={i} href={l.href} target="_blank" rel="noopener noreferrer"
                className="flex items-center gap-1.5 text-xs text-emerald-400/80 hover:text-emerald-400 transition-colors">
                <ExternalLink size={11} />{l.text}
              </a>
            ))}
          </div>
        </motion.div>

        <motion.div initial={{ opacity: 0 }} animate={{ opacity: 1 }} transition={{ delay: 0.7 }}
          className="mt-4 flex items-center justify-center gap-2 p-4 bg-emerald-500/5 border border-emerald-500/15 rounded-2xl text-sm text-white/50">
          <Mail size={14} className="text-emerald-400" />
          שאלות?&nbsp;<a href={`mailto:${META.email}`} className="text-emerald-400 hover:underline">{META.email}</a>
        </motion.div>
      </main>
      <Footer />
    </div>
  );
}
