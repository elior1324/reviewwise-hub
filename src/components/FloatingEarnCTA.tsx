/**
 * FloatingEarnCTA (Deprecated)
 *
 * ReviewHub no longer uses user-facing monetary incentives. This component is
 * intentionally a no-op and kept only to avoid accidental reintroduction via
 * stale imports in older branches.
 */

export default function FloatingEarnCTA() {
  return null;
}
